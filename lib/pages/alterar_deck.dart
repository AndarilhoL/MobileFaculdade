import 'package:flutter/material.dart';

class AlterarDeck extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Alterar Deck'),
      ),
    );
  }
}
